# Implementation Plans

Generated by the improve skill on 2026-08-07. Execute in the order below unless
dependencies say otherwise. Each executor: read the plan fully before starting,
honor its STOP conditions, and update your row when done.

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 001  | Wire the "View PDF" button in Document Vault | P1 | S | — | DONE |
| 002  | Remove hardcoded localhost API URL in FleetManager | P1 | S | — | DONE |
| 003  | Implement Iframe Traversal for ATS Scraper | P2 | M | — | DONE |

## Dependency notes

- None of these plans have explicit dependencies on each other.

## Findings considered and rejected

- **Missing `settings` page in Dashboard sidebar**: Rejected for now. `SettingsPage.tsx` does not exist in the codebase yet. Better to remove the navigation item entirely or implement a full settings feature separately rather than a quick bug fix.
- **GhostChat does not write to `candidate_memories`**: Rejected (Fixed). The audit doc identified this as a gap, but upon inspecting the codebase, the backend `chatController.ts` `/api/chat` route has already been updated to correctly extract and insert `new_memories` into the `candidate_memories` table. The frontend updates dynamically when visiting the Ghost Brain tab.
